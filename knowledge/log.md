# Update Log

## 2026-07-26
* **Update**: v0.6.1 — el gate reescrito (26 → 70 asserts + `okf_selfcheck_test.py`, que verifica que falle cuando debe), el disparador de scope creep con chequeo de existencia, `run-eval.py` distinguiendo "midió 0" de "falló", y la capa de futuro medida ([0014](decisions/0014-future-layer-measured.md)).
* **Update**: **Medida** la capa de futuro en dos conejillos con n=3 ([decisión 0014](decisions/0014-future-layer-measured.md)): un roadmap vigente y auto-contenido baja "¿qué sigue?" de 12.3 a 4.0 turnos (rangos sin solaparse, dispersión cero) y el harvest se corre solo y verifica; pero un roadmap desactualizado **cuesta más que no tenerlo**, y el disparador de scope creep podía escribir premisas falsas en el roadmap (arreglado con el chequeo de existencia, re-medido 3/3).
* **Update**: Corrido el **cold-review de 4 lentes** (gate de release) sobre 0.6.0: 2 blockers y ~12 majors, ninguno de diseño — todos en la capa de propagación e instalación. Cerrados: los `SKILL.md` salen del bundle (`docs/okf/`), la instalación mínima se recorta con marcadores en vez de prosa, el material instalado se volvió autosuficiente ([0013](decisions/0013-installed-material-is-self-sufficient.md)), §3.5 quedó como fuente única del alcance normativo (la clase la da el `type`, no la carpeta), y se cortó el link de [0011](decisions/0011-future-work-layer.md) a `_changes/` que habría roto el gate al primer harvest. Selfcheck: 26 → 68 asserts, más `okf_selfcheck_test.py` (18 inyecciones que verifican que el gate falle cuando debe).
* **Update**: Medido el costo permanente del sistema (el contrato es lo único que se carga en cada turno): mantenido en ~1600 tokens instalados, con assert de **presupuesto** en el selfcheck, dos **niveles de instalación** (completo/mínimo) en la `GUIDE`, y la tabla de qué tan fuerte es cada garantía (instrucción vs git vs auditoría) + canario cross-vendor en `install-per-tool.md`.
* **Update**: Los disparadores de la capa de futuro se movieron al `AGENTS.md` (que lee toda herramienta) para que el usuario **nunca tenga que nombrar un procedimiento**, más la regla "hablale en su idioma, no en OKF". Verificado con un agente en frío y un mensaje de usuario no técnico (ver [0011](decisions/0011-future-work-layer.md)).
* **Update**: La autoridad frente al código ahora depende del tipo de documento (nueva §3.5 de la spec): los descriptivos pierden contra el código, los **normativos** (decisión aceptada, convención, rumbo, cambio activo) lo obligan — una violación se reporta y se resuelve arreglando el código o superseder la decisión, nunca editándola en silencio. Se agrega el **Nivel 4 de verificación (Cumplimiento)**. Decisión [0012](decisions/0012-descriptive-vs-normative.md) generalizada.
* **Update**: Comparado el kit con [OpenSpec](https://github.com/Fission-AI/OpenSpec) (spec-driven development): nuevo [interop](../reference/spec-driven-interop.md), decisión [0012](decisions/0012-descriptive-vs-normative.md) (descriptivo vs normativo — el gap que destapó la comparación), escenarios CUANDO/ENTONCES en el template de cambio, y explorar-antes-de-comprometer + right-sizing en `okf-plan`. El kit se auto-aplica la capa: primer doc real en `_changes/`.
* **Update**: v0.6.0 — capa de futuro: el rumbo como concepto ([roadmap.md](roadmap.md)) + cambios efímeros en `_changes/` con harvest al cerrar (templates `_roadmap.md`/`_change.md`, skill `okf-plan`). Decisión en [0011](decisions/0011-future-work-layer.md); aclarada la spec §3.4 (intención vigente sí es estado presente; el plan de un cambio no es concepto).

## 2026-06-17
* **Update**: Auto-aplicado el enforcement del kit a sí mismo — CI (`.github/workflows/selfcheck.yml`) + pre-commit hook (`.githooks/`) que corren `okf_selfcheck`, y `CLAUDE.md` shim. Detalle en `DEVELOPING.md`.
* **Update**: v0.5.0 — cosechadas buenas prácticas de the-conclave: regla "gana el código", ciclo de deprecación (`status`/`supersedes`), patrón `_generated/`, header de frescura, `log.md` opcional, scratchpad; el linter ignora dirs con prefijo `_`.
* **Update**: Kit endurecido a v0.4.3 — revisión de 4 lentes + foolproofing; gate determinista (validador YAML stdlib, sin PyYAML); pre-commit hook seguro (sin `git stash`, no toca el working-tree); re-estampado `kit_version`.
* **Initialization**: Bundle OKF inicial creado con OKF kit v0.3.0 (de VERSION), dogfoodeando OKF sobre el propio kit.
* **Creation**: Sembrados los conceptos iniciales — `architecture/` (overview, modelo de tres capas), `concepts/` (bundle/concepto, progressive disclosure, perfiles, ciclo de vida), `decisions/` (0001 links relativos, 0002 consumo permisivo, 0003 kit_version vs okf_version, 0004 vendor-neutral, 0005 source of truth, 0006 perfil del dogfood), `runbooks/` (lint, cold test, bootstrap) y `references/` (formato OKF, Repomix).
